/// <reference types="vite/client" />

/** Injected at build time by vite.config.ts's `define` — see DataScreen's build-info footer. */
declare const __COMMIT_SHA__: string;
declare const __BUILD_TIME__: string;
